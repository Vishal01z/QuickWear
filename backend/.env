# Server Port
PORT=5000

# MySQL Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=1234
DB_NAME=quickwear

# JWT Secret Key (for login authentication tokens)
JWT_SECRET=quickwearSecretKey

# Optional: Timezone if needed by MySQL (not required for most)
DB_TIMEZONE=+00:00
